# 大纲层级

统一采用以下层级：

```text
全书总纲
  └─ 部纲
      └─ 卷纲
          └─ 剧情单元
              └─ 卷章节索引
                  └─ 单章章纲
                      └─ 正文
```

全书总纲一次覆盖所有部；部纲一次覆盖该部所有卷方向；卷章节索引一次覆盖该卷所有章。各层文件名、状态标记和必填结构统一读取 `references/guides/novel-project-schema.md`，不在本文件另设模板。
