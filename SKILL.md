---
name: novel-skill-master
description: 规划、创作、续写和收尾长篇小说，使用“总纲→部纲→卷纲→章纲→正文”的分层文件记忆管理人物、时间线与伏笔。用于用户要写小说、规划部/卷/章、在新会话续写已有小说、写正文、追踪或回收伏笔、检查大纲完整性、处理卷末部末或全书完结时。
---

# 小说创作总控

把正文质量放在中心，把规划变成正文可依赖的分层记忆。每次只深化当前需要的层级，但全书的所有“部”和“卷”必须先有经用户确认的方向，避免后半部只剩标题。

## 核心契约

1. 先询问，再定方向。总纲、部纲、卷纲和章纲中的关键走向均须由用户确认；用户未明确的重大转折不得擅自定案。
2. 逐层展开。严格按“总纲 → 全部部纲 → 全部卷方向 → 当前卷详纲 → 全卷章节索引 → 当前章详纲 → 正文”推进。
3. 生成即落盘。用户确认一个结构后，立即写入对应文件并运行结构校验；对话内容不能替代项目文件。
4. 完整才算完成。只有标题、占位符、待定项或省略号的文件均视为未完成。不得因为达到单次输出长度就标记阶段完成。
5. 长列表先索引后深化。先一次性列全所有部、卷或章的方向，再分批写详细内容；每批结束输出已完成范围和剩余范围。
6. 写前逐级回读。每次写正文都从全书、部、卷、章到动态状态依次读取；缺少任何必需层级时暂停写作并补齐。
7. 写后更新记忆。正文落盘后同步更新章节摘要、人物状态、时间线、伏笔和进度，供下一章读取。
8. 新会话先恢复。续写既有项目时先扫描全书结构和全部章节摘要，生成续写上下文；不得只读最近一章就直接写。

## 项目入口

先查找 `.webnovel/master_state.json` 和 `.webnovel/outline_manifest.json`：

- 已存在且用户要续写：先读取 `skills_part/08-写章引擎/novel-continue.md`，完成全书恢复，再进入写章。
- 已存在且用户只要查询或改纲：读取状态与对应层级文件，从最小受影响项处理。
- 不存在：先读取 `skills_part/04-项目初始化/novel-init.md`，完成题材、核心冲突、主要人物、结局倾向和规模采集；不得直接生成正文。

需要创建或检查部卷映射时，使用：

```text
python tools/outline_guard.py init --project-root <项目根> --parts <部数> --volumes-per-part <如 12,10,8>
python tools/outline_guard.py status --project-root <项目根>
```

目录、文件名和必填字段以 `references/guides/novel-project-schema.md` 为唯一准则。

## 强制工作流

### 1. 全书总纲与部方向

读取 `skills_part/05-卷纲规划/novel-book-outline.md`。先询问用户全书终点、部数和每部的大转折，再给出覆盖全部部的方向方案。所有部都必须包含起点、目标、核心冲突、不可逆转折、终点和向下一部的交接，确认后分别落盘。

完成标准：`全书总纲.md` 和每个 `第NN部-部纲.md` 均有实质内容，且结构校验通过。

### 2. 卷方向与卷详纲

读取 `skills_part/05-卷纲规划/novel-plan.md`。先为每一部列全所有卷的方向并让用户确认，再逐卷深化。卷方向阶段不得混入逐章细节；卷详纲负责事件链、人物弧、伏笔、高潮和卷末交接。

完成标准：清单中的每一卷都有独立文件；当前要写的卷标记为 `detailed-confirmed`。

需要剧情单元时再读取 `skills_part/06-剧情单元/novel-arc.md`，把当前卷拆为 3-6 个可执行单元。

### 3. 章节规划

读取 `skills_part/07-分章大纲/novel-chapter-outline.md`。先生成当前卷完整章节索引，每章至少有目标、冲突、转折/发现、结果和章末钩子；用户确认后按 5-10 章一批深化，每章写入独立章纲文件。

完成标准：当前章的独立章纲存在，相邻章的结果与起点能衔接。

### 4. 正文创作

读取 `skills_part/08-写章引擎/novel-write.md` 和 `skills_part/08-写章引擎/novel-foreshadowing.md`。正文是主任务；规划文件用于约束和供能，不能把正文写成大纲扩写或节点逐条翻译。

写前运行：

```text
python tools/outline_guard.py preflight --project-root <项目根> --part <部号> --volume <卷号> --chapter <章号>
```

只有返回 `ok: true` 才能落笔。用户明确要求试写时，可输出未落盘试稿，但必须说明它尚未进入正式项目记忆。

写前必须检查伏笔账本的回收窗口；写后必须检测本章的新埋设、强化和回收，并刷新埋设章前后章节摘要。没有到期伏笔时不强行处理。

### 5. 新会话续写

读取 `skills_part/08-写章引擎/novel-continue.md`。扫描器生成全书文件清单后，依次浏览全部部卷结构和所有已写章节摘要；缺失摘要则回读对应正文并补齐。完成 `continuation_context.md` 后，才按正文创作流程写下一章。

### 6. 审查与收尾

单章审查读取 `skills_part/09-质量审查/novel-review.md`。到达卷末、部末或全书结尾时，读取 `skills_part/10-收尾管理/novel-finish.md`，分别完成回收、状态结算、摘要和下一层交接；全书收尾必须核对主线、人物弧和承诺兑现。

## 写前记忆栈

按以下顺序读取，先宽后窄：

1. 全书层：`全书总纲.md`、`MASTER_SETTING.json`、风格约定。
2. 部层：当前 `第NN部-部纲.md`。
3. 卷层：当前 `第NNN卷-卷纲.md`、剧情单元文件。
4. 章层：卷章节索引、当前章纲、上一章正文或上一章收尾摘要。
5. 动态层：人物状态、时间线、伏笔账本及到期结果、项目经验和按需检索到的历史片段。
6. 会话层：新会话续写时读取 `continuation_context.md`，同时保留对原始层级文件的回读。

保持层次：全书文件只存全书事实，部文件只存部级变化，卷文件只存卷级事件链，章文件只存本章执行约束。新信息写到最低且足够的层级；影响上层方向时，再同步上层文件并记录变更原因。

## 用户交互

- 每轮只询问当前层级真正阻断的 1-3 个决策，附带可修改的推荐方案。
- 用户回答含糊时，先复述成可确认的方向，不把推测写成既定事实。
- 方案被否决时保留用户已确认部分，只重做受影响的层级和下游文件。
- 用户说“继续”时，先读状态和文件，再从最小未完成项继续；不得凭对话记忆重建项目。

## 路由

- 生命周期、状态和命令：读取 `master-basic.md`。
- 写章、检查、恢复和收尾命令：读取 `master-advanced.md`。
- 具体文件模板：读取 `references/guides/novel-project-schema.md`。
- 伏笔追踪或回收：读取 `skills_part/08-写章引擎/novel-foreshadowing.md`。
- 新会话续写：读取 `skills_part/08-写章引擎/novel-continue.md`。
- 文风、对白、场景或类型技法：只读取当前章节实际需要的对应指南，避免参考资料淹没正文任务。
